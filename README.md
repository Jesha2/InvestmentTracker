# InvestmentTracker
REact: header, Forms, CSS module, UseState, using map to list data to a table component,, 
lifting the state up to pass the data to parent, useSate all the  4 entries use one UseStatee

Use your terminal to cd into the code folder, and run npm i. T
his will see whatever dependencies are listed in the package.json file, download them from npm, and store that code in your node_modules folder.

Next, run npm start to start the react app
